{
  "name": "@rtamura30/aws-cost-ts",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/r-tamura/aws-cost-ts.git"
  },
  "bin": {
    "awscost": "./bin/awscost"
  },
  "dependencies": {
    "@slack/web-api": "^5.9.0",
    "@slack/webhook": "^5.0.3",
    "arg": "^4.1.3",
    "aws-sdk": "^2.686.0",
    "date-fns": "^2.14.0"
  },
  "devDependencies": {
    "@types/jest": "^25.2.3",
    "jest": "^26.0.1",
    "ts-jest": "^26.1.0",
    "typescript": "^3.9.3"
  },
  "scripts": {
    "start": "tsc && ./bin/awscost",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest --watch"
  }
}
